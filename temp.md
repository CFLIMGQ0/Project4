# temp.py 详细说明

## 1. 脚本职责

`temp.py` 当前只实现**第一轮清洗**，用于遍历数据集目录、识别缺失 `img/` 或 `pdf/` 的检查目录，并输出该轮清洗后的统计结果。

当前脚本产出两类结果：

1. 患者级有效性表格；
2. 第一轮清洗统计报告。

后续如果继续做第二轮、第三轮清洗，可在此基础上继续追加轮次逻辑；当前版本**不预设后续分类任务**，相关任务状态统一视为“暂定”。

## 2. 路径配置来源

项目中所有默认路径统一维护在 `configs/path.yaml`：

- `paths.dataset_root`：原始数据根目录；
- `paths.output_dir`：输出目录；
- `paths.patient_validity_table`：患者有效性表格输出文件；
- `paths.cleaning_report`：清洗统计报告输出文件。

若命令行显式传入同名参数，则以命令行参数为准。

## 3. 第一轮清洗逻辑

### 3.1 患者目录识别

- 数据根目录下的一级子目录会被视为患者目录；
- 患者目录名称默认期望以 `ZS` 开头；
- 若命名不符合约定，不会阻止统计，但会被记录为异常命名患者。

### 3.2 检查目录识别

- 每个患者目录下的直接子目录会被视为一次检查；
- 单次检查目录同时存在 `img/` 与 `pdf/` 时，判定为保留检查；
- 缺少任一子目录时，判定为第一轮清洗中被去掉的检查目录。

### 3.3 第一轮清洗后的统计口径

脚本会同时输出：

- **原始统计结果**：基于患者目录下全部直接子目录计算；
- **第 1 轮清洗后的统计**：去掉缺失 `img/` 或 `pdf/` 的检查目录后重新统计。

当前特别关注的字段包括：

1. 患者总数；
2. 检查总次数；
3. 空患者目录数；
4. 命名异常患者目录数及样例；
5. 缺失 `img/` 或 `pdf/` 的检查目录数及样例；
6. 患者检查次数分布；
7. **第 1 轮清洗后检查次数为 0 的患者数**。

其中，第 7 项正是这一轮清洗的核心统计结果：即去掉缺失 `img/` 或 `pdf/` 的检查目录后，有多少患者最终变成 `0` 次检查。

## 4. 患者有效性表格

脚本会生成三列表格：

| patient_dir | is_valid | invalid_reason |
| --- | --- | --- |
| 患者目录名 | 1/0 | 无效原因说明 |

判定规则如下：

- 只有在**第 1 轮清洗后该患者检查次数为 0** 时，才会标记为无效；
- 此时 `is_valid = 0`；
- `invalid_reason` 写为 `第 1 轮清洗后该患者检查次数为 0`；
- 其余患者保留为有效，`invalid_reason` 为空。

## 5. 清洗统计报告

清洗统计报告会记录：

- 原始患者数与原始检查数；
- 第 1 轮去掉的检查目录数；
- 第 1 轮清洗后检查次数为 0 的患者数；
- 第 1 轮清洗后保留的患者数与检查数。

这份报告当前只描述**第一轮清洗**，便于后续多轮清洗时继续追加新轮次内容。

## 6. 使用方式

### 6.1 使用默认路径配置运行

```bash
python temp.py
```

### 6.2 覆盖 path.yaml 中的路径

可通过命令行参数覆盖默认配置，例如分别指定数据根目录、患者有效性表格输出文件和清洗统计报告输出文件。

### 6.3 指定其他路径配置文件

如需切换到其他配置文件，可通过 `--config` 参数传入目标配置文件。

## 7. 当前维护边界

- 当前阶段以**数据集整理与多轮清洗准备**为主；
- 分类任务、建模任务等内容暂不展开，统一标记为“暂定”；
- 除本文件外，其他说明文档暂不展开 `temp.py` 的细节，待后续脚本命名和逻辑稳定后再统一补充。
