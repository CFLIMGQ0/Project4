# task1 论文结构概览

## 论文定位

这篇初稿按 `Journal of International Medical Research` 的观察性研究写法组织，主题聚焦于：

**基于常规胃镜检查图像包与报告弱标签的多标签分类模型构建与内部验证。**

核心方法是 `label graph MIL`，即：

- 先对同一次检查中的多张图像做实例编码；
- 再为不同标签分别做注意力聚合；
- 最后通过标签关系图传播建模标签之间的共现关系。

---

## 各章节概括

### Abstract

主题是什么：
基于报告弱标签的胃镜多标签分类模型做了什么、结果如何。

大致介绍了啥：
用结构化摘要形式写了研究目标、数据规模、模型设计、内部测试结果，以及主要结论。这里给出了最关键的指标，包括 `Macro F1=0.8650`、`Micro F1=0.8616`，并明确说明模型优于当前项目中的 baseline 和适配 SOTA 对照。

### 1. Introduction

主题是什么：
为什么这个问题值得做，为什么要用多实例学习和标签关系建模。

大致介绍了啥：
先说明胃镜检查天然是“单次检查包含大量图像、但标签通常只在报告层面出现”的弱监督场景；再指出现有内镜 AI 工作更多是单图像或单病灶任务，难直接适配当前项目的检查级多标签任务；最后引出本文方法，强调多标签共存场景下，单纯独立预测每个标签不够，因此需要加入标签图推理模块。

### 2. Materials and methods

主题是什么：
数据怎么来、标签怎么定、模型怎么搭、实验怎么做。

大致介绍了啥：
这一章是全文方法核心，分为几个小部分：

- `Study design and reporting guideline`：说明这是单中心、回顾性、观察性研究，并按 `STROBE` 思路撰写。
- `Dataset construction and eligibility`：介绍数据来自清洗后的检查级目录与报告表；说明胃镜记录的识别方式、三类标签的文本匹配规则、纳排标准，以及最终样本规模。
- `Model development`：详细描述 `ConvNeXt-Tiny + 多标签 gated attention + label graph reasoner + 标签分类头` 的整体流程，并写了关键公式。
- `Training and evaluation`：说明训练配置，包括 `6:2:2` 切分、`batch size=12`、`max instances=16`、`AdamW`、`asymmetric loss`、`mixed precision` 等。
- `Comparator models and ablation settings`：说明对照实验包含 5 个 baseline、5 个适配 SOTA，以及两个消融实验（注意力维度、bag 大小）。

### 3. Results

主题是什么：
模型效果到底怎样，以及和现有项目中的其他方法相比处于什么水平。

大致介绍了啥：
这一章先给出主模型在内部测试集上的总体表现，再展示标签级结果，然后给出与 baseline / SOTA 的横向对比，最后补充消融实验结果。文中放了 5 张表：

- `Table 1`：数据集统计；
- `Table 2`：主模型在不同 checkpoint 选优标准下的整体结果；
- `Table 3`：主模型三个标签各自的性能；
- `Table 4`：与 baseline 和适配 SOTA 的对比；
- `Table 5`：注意力维度和 bag 大小的消融结果。

### 4. Discussion

主题是什么：
这些结果意味着什么，模型为什么有效，还存在哪些问题。

大致介绍了啥：
先解释模型有效的原因，重点围绕“同一次胃镜检查里多标签共现比例高，因此标签关系建模是合理的”；再说明这个模型更贴近实际临床流程，因为它直接面向“检查级”而不是“单张图像级”；最后明确写出局限性，包括：

- 单中心、回顾性、仅内部验证；
- 标签来自报告规则，不是所有病例都有病理金标准；
- 当前是检查级随机切分，不是患者级切分；
- 对照实验 backbone 尚未完全统一；
- 还缺少外部验证和更严格统计检验。

### 5. Conclusions

主题是什么：
论文最后想留下的核心结论。

大致介绍了啥：
总结为一句话就是：当前项目已经证明，基于常规胃镜多图像检查和报告弱标签，做检查级多标签分类是可行的，而 `label graph MIL` 在项目现有实验里表现最好；但在正式投稿前，还需要补齐伦理信息、外部验证和更严格的泛化评估。

### Author contributions

主题是什么：
作者分工怎么写。

大致介绍了啥：
目前写成了占位版，说明了谁负责研究设计、谁负责实现与实验、谁负责临床解释与修订。正式投稿前需要替换为真实作者姓名与贡献。

### Statements and declarations

主题是什么：
投稿期刊要求的声明性内容。

大致介绍了啥：
这里补了投稿时必须有的几个部分，包括：

- `Ethical considerations`
- `Consent to participate`
- `Consent for publication`
- `Declaration of conflicting interests`
- `Funding`
- `Data availability`
- `Acknowledgements`

其中伦理审批号、知情同意、基金信息目前都明确保留为“待补充”，避免初稿里出现虚构信息；冲突声明、数据可得性和致谢则给出了可直接修改的标准写法。

### References

主题是什么：
论文当前引用了哪些类型的文献。

大致介绍了啥：
参考文献目前主要覆盖四类内容：

- 胃肠镜 AI / 深度学习相关背景文献；
- 上消化道内镜早期代表性深度学习工作；
- 多实例学习及其代表方法；
- 观察性研究报告规范 `STROBE` 与主干网络 `ConvNeXt`。

这部分已经足够支撑初稿写作，但正式投稿前仍建议你再结合目标期刊偏好补一轮更贴近胃镜临床应用的参考文献。

---

## 当前初稿的使用建议

这版初稿已经能作为“投稿起草版”继续往下推进，但正式投稿前建议优先补这几类信息：

1. 真实作者、单位、通讯作者信息。
2. 医院名称、研究时间范围、伦理审批号、是否豁免知情同意。
3. 若有的话，补充更公平的 matched-backbone 对照实验。
4. 若条件允许，补患者级切分或外部验证结果。
5. 再润色一次医学写作语气，并按期刊要求核对参考文献格式。
