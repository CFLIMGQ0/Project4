# 数据集结构说明

## 1. 数据集根目录

项目默认使用的原始数据根目录并不直接写死在文档中，而是统一记录在 `configs/path.yaml` 的 `paths.dataset_root` 参数内。

因此，若需要切换数据环境、迁移服务器或调整输出位置，请优先修改 `configs/path.yaml`。

## 2. 患者级目录结构

每个患者一个目录，目录名格式预期为：

```text
ZSxxxxxxxx
```

其中：

- `ZS` 为固定前缀；
- 后续字符通常为患者编号；
- 脚本会优先按该格式识别，但不会强制拒绝其他目录名，只会在统计中提示异常命名样例。

## 3. 检查级目录结构

每个患者目录下包含 `1~n` 个检查目录。每个检查目录表示该患者的一次检查，可能对应：

- 胃镜
- 无痛胃镜
- 超声胃镜
- 染色胃镜
- 肠镜
- 其他扩展检查

检查目录下应至少包含两个子目录：

```text
检查目录/
├── img/
└── pdf/
```

其中：

- `img/`：保存该次检查产生的图片；
- `pdf/`：保存该次检查对应的 PDF 报告。

## 4. 当前统计目标

项目当前主要围绕数据结构检查与清洗准备，目标包括：

1. 统计患者总数；
2. 统计总检查次数；
3. 汇总患者检查次数分布；
4. 识别缺少 `img` 或 `pdf` 的异常检查目录；
5. 输出患者级有效性表格，方便人工筛查清洗后无检查记录的患者；
6. 输出清洗统计报告，记录清洗前后患者数量对比。

## 5. 统计与输出说明

关于 `temp.py` 的详细统计口径、路径读取方式、输出文件内容以及患者有效性表格规则，请统一查看 `temp.md`。

本文档仅描述数据组织方式，不重复展开 `temp.py` 的实现细节。

## 6. 使用建议

- 在正式清洗或建模前，先运行 `python temp.py` 获取当前数据结构概览；
- 若需要调整默认数据根目录或输出路径，请先修改 `configs/path.yaml`；
- 若后续还需要统计图片数、PDF 数、每种检查类型占比，可在当前脚本基础上继续扩展；
- 若涉及脚本行为解释，请统一引用 `temp.md`。
