# 患者级标签与图像级混杂问题处理方案（第三版）

**创建日期**：2026-03-31  
**适用场景**：患者级报告标签下，一个检查目录内混有多种图像模态/子类型，例如白光胃镜、染色内镜、超声胃镜、对比图等。  
**核心矛盾**：你的标签是**患者级 / 检查级**，但模型真正看到的是**图像级实例**；两者粒度不一致，所以会天然产生“假噪声”“跨模态混入”和“弱监督错配”问题。

---

## 一、先给结论

### 1. 你现在想到的两个方案都**有价值，但都不应该单独成为主线**

- `check_similarity` 可以继续用，但它只能做**粗粒度预分析**，不能直接解决同一检查目录内的细粒度标签问题。
- 全量手工清洗也可以做，但不应该一开始就对全量数据硬清洗、硬转类、硬删除，否则成本极高，而且很容易把“对患者级任务其实有用的图像”过早删掉。

### 2. 你现在最需要做的不是“立刻删图”，而是先建立三层标签体系

建议把每张图像的状态拆成三层：

1. **患者级主标签**：来自 `reportTitle`，保留不动。  
2. **图像级模态标签**：如白光胃镜、染色、超声、其他、低质量、无法判断。  
3. **样本可用性标签**：`患者级任务可用 / 图像级任务可用 / 仅人工复核 / 直接剔除`。

这一步比“把图像搬到不同文件夹”更重要。  
**建议先维护 `manifest.csv` / `parquet` 元数据表，而不是先移动原始文件。**

---

## 二、对你现有思路的评价

### 方案 1：基于 `check_similarity` 划分类型

**评价**：可作为前处理工具，但不能作为最终方案。

**理由**：

- 现有 [`scripts/temp_check_similarity.py`](/home/Lim/Project4/src/scripts/temp_check_similarity.py) 的输入组织方式是按 `reportTitle` 汇总图像，再计算不同 `reportTitle` 间的分布差异。
- 它当前做的是**类间分布分析**，不是**类内实例路由**。
- 它可以回答：
  - “无痛胃镜”和“超声胃镜”整体像不像？”
  - “哪些 `reportTitle` 之间高度相似，可能应该合并或重点排查？”
- 它不能可靠回答：
  - “这个具体检查目录里，哪几张其实是染色图，哪几张是白光图？”
  - “这张被筛出来的图像到底应该重新归到哪个细类别？”

**所以正确定位应该是**：

- 用它做**粗筛和发现问题对**；
- 不要指望它直接给出图像级真标签。

### 方案 2：手动清洗 + 算法辅助剔除

**评价**：方向对，但需要改成“人机协同标注闭环”，不能做成“全量搬运 + 全量人工判定”。

你担心的两个问题都是真问题：

#### 问题 A：被剔除的图像到底还要不要用？

我的建议不是“剔除/不剔除”二选一，而是分成 4 个池：

| 样本池 | 含义 | 是否保留文件 | 是否用于图像级训练 | 是否用于患者级训练 |
|---|---|---:|---:|---:|
| `invalid` | 损坏、非医学、严重模糊、无诊断价值 | 否/隔离 | 否 | 否 |
| `cross_modal` | 医学上有效，但与继承标签不一致 | 是 | 暂不直接用，待重标或软标签 | 是，通常可保留 |
| `ambiguous` | 模态不清、边界样本、模型不确定 | 是 | 先不用作硬监督，可作无标签/半监督 | 可保留 |
| `clean` | 与标签体系一致、质量可接受 | 是 | 是 | 是 |

**关键原则**：

- 不要在第一轮就大量物理删除图片。
- 对患者级任务而言，很多“异类图像”不一定是垃圾，可能恰好反映了真实检查流程。
- 真正应该删除的，只是 `invalid`。

#### 问题 B：手动清洗完后，患者级任务怎么训练？

这是可行的，而且并不一定非要做“多个分类器串联再集成”。

更稳妥的做法是：

1. **患者级任务**：仍然按一个检查目录作为一个样本，用 MIL 或 bag-level 模型处理。  
2. **图像级任务**：用人工清洗出的少量高质量图像训练模态分类器。  
3. **两条线并行**：图像级分类器负责给图像打细标签，患者级模型负责最终诊断/分类。  
4. **后期再融合**：把图像级模态预测作为患者级模型的额外输入，而不是一开始就硬做复杂集成。

也就是说：

- 你不需要先把全量数据“清洗到完全纯净”才开始训练。
- 你只需要先做一个**小而干净的金标准子集**，剩余大部分数据仍然可以用弱监督方式参与训练。

---

## 三、我建议你先做的基础设施，而不是先删图

### 1. 先建立 `manifest`，不要先平铺文件夹

建议先建一张总表，至少包含这些字段：

```text
patient_id
exam_id
exam_dir
image_path
report_title
organ
coarse_patient_label
image_modality_label
image_modality_source
image_modality_confidence
review_status
use_for_image_cls
use_for_patient_cls
split
notes
```

其中：

- `coarse_patient_label`：原始患者级标签；
- `image_modality_label`：图像级细标签，允许为空；
- `image_modality_source`：`manual / pseudo / transferred / unknown`；
- `review_status`：`clean / ambiguous / cross_modal / invalid`；
- `use_for_image_cls`、`use_for_patient_cls`：把“是否参与训练”显式记录下来。

**这样做的好处**：

- 不破坏原始目录结构；
- 后续转类、回滚、追溯都容易；
- 同一张图既可以“图像级不用”，又可以“患者级继续保留”。

### 2. 先定义 4-6 个稳定的图像级标签，不要一开始就追求特别细

你现在最需要的不是几十个图像级标签，而是先把最主要的视觉模态分开。  
建议第一版先定义：

- `白光胃镜`
- `染色/色素内镜`
- `超声胃镜`
- `肠镜`
- `低质量/无效图`
- `其他/无法判断`

如果后续发现某一类内部仍然异质，再往下细分。

### 3. 划分数据时必须按患者/检查切分，不能按图像随机切

否则同一个患者的图像会同时进入训练集和验证集，结果会严重泄漏。

---

## 四、推荐方案（至少 4 个）

## 方案 A：患者级多实例学习 MIL 主线

**类型**：成熟方案，借鉴计算病理学和弱监督医学影像  
**参考脉络**：ABMIL、CLAM、TransMIL、SmMIL

### 核心思想

把一个检查目录看成一个 `bag`，目录中的每张图像是 `instance`。  
模型只用患者级标签训练，但通过注意力机制自动学习“哪几张图更关键”。

### 为什么适合你

- 你的原始标签本来就是患者级；
- 一个检查目录里混有多种图像，本来就很像 MIL 的典型设定；
- 这条路线**不要求你先把图像全部清洗干净**。

### 落地方式

1. 用预训练视觉骨干提取每张图像特征：
   - 医学优先：BiomedCLIP / MedImage 相关骨干；
   - 通用优先：DINOv2 / ViT。
2. 每个检查目录内，对所有图像特征做 attention pooling。
3. 输出患者级预测。
4. 注意力权重可作为“目录内哪些图像最像主模态”的解释信号。

### 优点

- 不依赖全量图像级标注；
- 非常贴合你的真实标签粒度；
- 可以直接作为患者级任务的强基线；
- 可以顺便产出“重点图像”和“可疑图像”排序，辅助后续清洗。

### 局限

- 不能天然得到特别可靠的图像级模态标签；
- 如果你最终目标是“构建纯净图像级数据集”，它不够；
- 需要控制每个 `bag` 的图像数量和采样策略。

### 适用目标

- 目标是**患者级分类/诊断**；
- 希望尽快做出一个能用的主线模型；
- 不想一开始就陷入全量人工清洗。

---

## 方案 B：小规模金标准 + 噪声标签学习

**类型**：成熟方案，借鉴 WebVision / Clothing1M / 医学噪声标签处理  
**参考脉络**：Co-teaching、DivideMix、SOP、Confident Learning

### 核心思想

把“图像继承患者级标签但部分不一致”视为**噪声标签学习**问题。  
先做一个小规模高质量图像级金标准集，再用噪声鲁棒训练处理剩余大规模数据。

### 为什么适合你

- 你确实存在大量“标签不完全错、但粒度不匹配”的样本；
- 完全人工清洗成本太高；
- 完全不清洗又会污染图像级分类器。

### 落地方式

1. 从最混乱的 4-6 个 `reportTitle` 中，先抽样标注一批图像级金标准。
2. 用金标准集训练一个初始图像级模态分类器。
3. 对全量图像跑预测，得到每张图的模态概率和不确定性。
4. 对继承标签和模型预测严重冲突的样本，标记为 `cross_modal` 或 `ambiguous`。
5. 用 DivideMix / Co-teaching 这类方法，把高可信样本当监督、低可信样本当半监督或软标签样本。

### 优点

- 能得到图像级标签和置信度；
- 非常适合做“图像级数据集净化”；
- 有成熟方法可直接借鉴；
- 也方便后续给患者级模型提供细粒度模态输入。

### 局限

- 仍然需要一批人工金标准作为锚点；
- 如果混杂比例极高，初期伪标签会不稳；
- 这条路线更适合图像级任务，不是患者级任务的最直接解法。

### 适用目标

- 目标是**构建干净的图像级模态分类器**；
- 目标是**建立可复用的细粒度图像标签体系**；
- 你愿意先做一小批人工标注。

---

## 方案 C：患者约束的自监督原型图精炼框架

**类型**：偏 SOTA，适合论文方向  
**关键词**：自监督特征 + 聚类原型 + 图约束 + EM/伪标签迭代

### 核心思想

先完全不信任粗粒度标签，先用自监督视觉基础模型学出稳定表征；  
然后在特征空间里自动发现“图像原型簇”，再利用患者级标签作为**软约束**去精炼实例级标签。

### 一个更适合你数据的表述

你的问题不是普通 noisy label，而是：

- **bag 级标签真实存在**
- **instance 级标签缺失且混杂**
- **同一 bag 内可能有多个真实模态**

所以更准确的建模应该是：

> “患者级弱监督下的潜在模态发现与实例级标签精炼”

### 建议框架

1. 用 DINOv2 / BiomedCLIP 抽取全量图像特征。
2. 建立全量图像的 kNN 图和原型簇：
   - HDBSCAN / GMM / 谱聚类；
   - 每个簇对应一个潜在模态原型。
3. 建立“簇 - 患者级类别”的二部图，统计每个簇在不同 `reportTitle` 下的出现分布。
4. 做一次带约束的伪标签推断：
   - 视觉上接近同一原型的图像应有相似模态标签；
   - 同一检查目录的模态组合应和其患者级标签语义一致，但不是硬一致；
   - 对不确定样本输出分布而不是硬标签。
5. 迭代更新：
   - 原型更新；
   - 伪标签更新；
   - 小量人工修正高价值簇。

### 这个方案的创新点

- 不是简单做 noisy label，也不是简单做 MIL；
- 明确把“患者级监督”和“实例级潜在模态发现”联合起来；
- 很适合写成“从粗粒度检查标签到细粒度图像标签自动精炼”的论文框架。

### 论文价值点

- 可以强调**标签粒度不匹配**而非一般标签错误；
- 可以强调**多模态混入**而非普通类内噪声；
- 可以强调**原型图 + bag 约束 + 不确定性输出**的联合建模。

### 风险

- 算法设计复杂度高；
- 需要较强的实验设计和消融；
- 前期没有小规模金标准时，评估会比较困难。

---

## 方案 D：潜变量图像路由 + 专家 MIL（Latent Routing MoE）

**类型**：偏 SOTA，适合论文方向  
**关键词**：Router、Mixture-of-Experts、bag 监督、实例路由、不确定性

### 核心思想

让模型在训练时自动学习：

- 每张图像更像哪种模态；
- 每种模态该交给哪个专家网络处理；
- 最后再聚合为患者级预测。

换句话说，就是把“先分图，再做患者级判断”变成一个**联合学习**问题。

### 结构示意

```text
图像 x_i
  -> Router：输出模态分布 p(modality | x_i)
  -> 按概率送入不同 Expert
  -> 各 Expert 提取特征
  -> Patient-level Aggregator 聚合
  -> 输出患者级预测
```

### 为什么这条路线适合发论文

因为它不再把“图像清洗”和“患者级建模”拆成两个孤立步骤，而是统一起来：

- Router 学“图像该归哪类”
- Experts 学“同类图像怎么建模”
- Aggregator 学“患者最终怎么判”

### 可以加入的关键约束

- **熵约束**：Router 不要把所有图像都平均分给所有专家；
- **语义先验约束**：例如某些 `reportTitle` 通常以某种模态为主，但允许少量其他模态出现；
- **一致性约束**：同一检查目录中视觉极相似的图像，其路由结果应接近；
- **不确定性建模**：对难例输出 “不确定”，而不是硬分流。

### 优点

- 端到端；
- 兼顾图像级模态识别和患者级任务；
- 天然有解释性；
- 如果实验做扎实，论文味道比较足。

### 局限

- 比方案 C 更难训；
- 需要更谨慎的正则设计，否则 Router 容易塌缩；
- 对算力、实验次数和调参能力要求更高。

---

## 五、如果只能选一条现实主线，我建议这样组合

### 最务实的主线

**主线建议**：`方案 A + 方案 B`

也就是：

1. **先用 MIL 把患者级任务跑通**，不要等数据完全清洗后再开始。  
2. **同时做一个小规模图像级金标准集**，用噪声标签学习逐步清洗和重标。  
3. 后期再把图像级模态结果作为患者级模型的辅助特征。  

这是目前最稳、最不容易把项目拖死的路线。

### 如果你主要想发论文

**论文主线建议**：`方案 C 或 方案 D`  

但我仍然建议先做 `方案 A` 作为基线，因为：

- 没有强基线，创新方法很难证明有效；
- 没有图像级金标准子集，论文评估不扎实；
- 没有患者级基线，审稿人会质疑你是否真的解决了原任务。

---

## 六、你接下来到底该怎么做

## 第一阶段：先把标签体系和金标准子集搭起来

- [ ] 建立 `manifest.csv`，不要先移动原始文件。
- [ ] 定义第一版图像级标签体系，先控制在 4-6 类。
- [ ] 从最混乱的类别里抽样，先人工标 1500-3000 张图像。
- [ ] 标注时同时记录：
  - `image_modality_label`
  - `review_status`
  - `是否可用于患者级任务`
  - `是否可用于图像级任务`
- [ ] 按患者划分训练/验证/测试，避免泄漏。

## 第二阶段：先做两个基线

- [ ] **患者级基线**：做一个简单 MIL 模型。
- [ ] **图像级基线**：用金标准集训练一个图像级模态分类器。
- [ ] 在全量图像上跑推理，得到每张图的：
  - 模态概率
  - 不确定性
  - 与患者级标签的冲突分数

## 第三阶段：开始“清洗”，但不是硬删除

- [ ] 只对 `invalid` 做真正剔除。
- [ ] 对 `cross_modal`：
  - 图像级训练先不用硬标签；
  - 患者级训练先保留。
- [ ] 对 `ambiguous`：
  - 进入人工复核池；
  - 或作为半监督/无标签样本使用。
- [ ] 对高置信度可重分配图像：
  - 先在 `manifest` 里改标签；
  - 不急着物理移动文件。

## 第四阶段：决定项目走向

### 如果你的目标是“患者级分类效果优先”

- [ ] 以 MIL 为主线；
- [ ] 图像级模态分类器只作为辅助解释工具；
- [ ] 不必执着于把所有图像彻底净化。

### 如果你的目标是“做一个纯净图像级数据集”

- [ ] 以方案 B 为主线；
- [ ] 扩充金标准集；
- [ ] 做主动学习，把最不确定样本优先交给人工；
- [ ] 最终导出图像级净化版本数据集。

### 如果你的目标是“做论文”

- [ ] 先完成 A/B 基线；
- [ ] 再做 C 或 D；
- [ ] 准备以下评估：
  - 图像级分类指标；
  - 患者级分类指标；
  - 不确定样本识别能力；
  - 人工复核成本下降比例；
  - 各模态混杂比例统计。

---

## 七、我不建议你现在做的事

- 不建议一上来把所有病人的图像都平铺到大文件夹里人工翻。
- 不建议一开始就大量物理删除“看起来不对”的图像。
- 不建议直接拿患者级标签去训练图像级分类器，然后把输出当真值。
- 不建议在没有金标准子集的情况下讨论复杂集成学习。
- 不建议按图像随机切分训练/验证集。

---

## 八、一个更现实的判断

如果你的最终任务是**患者级分类**，那么“一个检查目录内出现多种图像”并不一定是问题本身，反而可能是任务定义的一部分。  
这时候最合理的方向不是追求“所有图像都纯净”，而是：

- 保持患者级监督；
- 让模型学会从混合图像中提取有效证据；
- 再把图像级模态识别作为辅助模块。

如果你的最终任务是**图像级模态分类/图像级病灶分类**，那就必须做细粒度图像标签体系，且必须引入一小批人工金标准样本。  
这时候最合理的方向不是“全量手工清洗”，而是：

- 先做金标准子集；
- 再做噪声鲁棒训练；
- 再做主动学习闭环。

---

## 九、推荐优先级

### 优先级 1：一定先做

- [ ] `manifest` 元数据表
- [ ] 图像级第一版标签体系
- [ ] 小规模金标准子集
- [ ] 患者级 MIL 基线

### 优先级 2：随后做

- [ ] 图像级模态分类器
- [ ] 全量伪标签与不确定性打分
- [ ] `cross_modal / ambiguous / invalid / clean` 四池拆分

### 优先级 3：论文方向再做

- [ ] 患者约束的自监督标签精炼
- [ ] 潜变量路由 + 专家 MIL

---

## 十、可以直接借鉴的参考文献 / 方向

### 成熟路线参考

- Attention-based Deep Multiple Instance Learning, ICLR 2018  
- CLAM: Data-efficient and weakly supervised computational pathology on whole-slide images, Nature Biomedical Engineering 2021  
- Co-teaching: Robust training of deep neural networks with extremely noisy labels, NeurIPS 2018  
- DivideMix: Learning with Noisy Labels as Semi-supervised Learning, ICLR 2020  

### 偏新 / 可借鉴的研究脉络

- DINOv2: Learning Robust Visual Features without Supervision, 2023  
- Learning from Label Proportions, NeurIPS 2020 / 2022 / 2023 系列  
- Partial-Label Contrastive Representation Learning for Fine-Grained Biomarkers Prediction From Histopathology Whole Slide Images, IEEE JBHI 2025  
- E2-MIL / uncertainty-aware MIL 一类工作，强调实例级不确定性与可解释性

---

## 十一、最终建议

**一句话版**：

不要把当前问题理解成“脏数据清洗”这么简单，它本质上是**患者级弱监督下的图像级标签粒度错配**。  
因此最稳妥的路线不是“全量硬清洗”，而是：

1. 先保留患者级标签体系；  
2. 先做小规模图像级金标准；  
3. 同时跑患者级 MIL 基线；  
4. 再用噪声标签学习或自监督精炼去逐步补全图像级标签；  
5. 真正只删除 `invalid`，其余样本先分池管理，不要过早物理删除。  

如果你愿意，我下一步最值得做的不是继续写方案，而是把：

- `manifest` 设计出来；
- 图像级标签字段定下来；
- 再补一个“第一轮人工标注抽样方案”。
