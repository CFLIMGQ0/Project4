# AGENTS.md（给 Claude 的开发规矩）

## 语言规范（必须遵守）
- 与用户沟通说明：使用中文（简体）。
- 生成或修改说明文档：使用中文（例如 README.md、DATASET.md、AGENTS.md、注释说明等）。
- 拉取请求时的 Conversation 内容也使用中文进行描述。
- 任何“执行日志/过程说明/变更摘要/检查结果说明”（例如：任务日志里的说明文字、Checklist、总结段落）：也必须使用中文。
- 代码内部的调试日志（print/logging）可以英文或中英混排，但面向用户的说明必须中文。
- 命令行参数、配置字段（config.yaml）：保持英文键名不变（避免接口不一致）。
- 对修改的操作结合用户的要求做一个详细总结。
- 如果任务涉及遍历大量数据（如批量扫描文件/样本），默认应加入进度条展示处理进度。

## 项目目录结构规范（必须遵守）

新项目默认采用统一的四目录结构。项目根目录通常形如 `Projectx/`，其下包含 `datasets/`、`outputs/`、`pre_weights/` 和 `src/` 四个一级目录：

```text
Projectx/
├── datasets/
│   └── main_data/
├── outputs/
├── pre_weights/
└── src/
```

各目录职责如下：

- `datasets/`：数据集根目录，用于存放项目使用的数据，不放项目源码。
- `datasets/main_data/`：主数据集目录，默认作为训练、验证、测试数据的主要读取路径。
- `outputs/`：输出目录，用于存放训练结果、测试结果、日志、checkpoint、图表、分析文件和其他实验产物。
- `pre_weights/`：预训练模型权重目录，用于存放外部下载或预先训练好的模型参数文件。
- `src/`：项目代码目录，用于存放训练、测试、模型、数据处理、可视化和工具脚本等源码。

后续开发时应默认遵守以下约定：

- 读取数据时，默认从 `datasets/main_data/` 开始组织路径。
- 保存任何实验产物时，默认写入 `outputs/`，不要写入 `src/`。
- 读取预训练权重时，默认从 `pre_weights/` 中查找。
- 新增代码文件、配置文件和脚本时，默认放在 `src/` 下，除非用户明确指定其他位置。
- 脚本中的路径参数可以保持英文键名，例如 `data_root`、`output_dir`、`pretrained_path`，但默认值应符合上述目录结构。
