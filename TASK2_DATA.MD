# TASK2_DATA

## 本次处理对象

- 目标文件：`/home/Lim/Project4/datasets/valid_dicts_report_for task2.csv`
- 本次仅处理 `watch` 列
- `watch_num` 保留原值，不做修改

## 本次处理结果

- 总行数：3447
- 原始 `watch` 中含 `|` 的行数：3440
- 去除完全重复/规范化重复后，真正存在非同文本冲突的行数：419
- 其中低相似度、需要人工指定合并结果的难例：27
- 合并后再次清洗模板残句/占位符的行数：62
- 最终 `watch` 中残留 `|` 的行数：0
- 最终残留明显模板占位符的行数：0

## 合并原则

### 1. 完全重复先去重

对同一行 `watch` 中只是重复拷贝、空格差异、标点轻微差异的版本，直接去重，保留单一文本。

### 2. 阳性信息优先于弱信息和缺失信息

本次按以下思路处理冲突：

- 明确有病变/异常 > 可能有/可疑 > 未提及/正常/无异常
- 明确描述病灶位置、大小、处理方式的版本，优先于只有背景描述的版本
- 如果一个版本写“未见肿块及息肉”，另一个版本明确写出息肉/隆起/糜烂/肿物，则保留阳性描述

### 3. 结合同一行其他字段辅助判断

合并时参考了同一行的：

- `watchResult`
- `specimen`
- `suggest`

主要用途：

- 判断某一版本是否遗漏了阳性病变
- 判断是否需要保留活检、咬除、ESD/STER/EMR 等操作信息
- 判断是否存在“术中肠镜/超声所见/手术记录”等互补段落需要拼接

### 4. 互补段落做拼接，不强行二选一

对以下情况，采用“合并段落”的方式处理，而不是只保留其中一个版本：

- `内镜所见` + `超声所见`
- `内镜所见` + `手术记录`
- `术中肠镜` + 上消化道手术记录

也就是说，只要两个版本描述的是同一次检查中的不同组成部分，就尽量拼成一条完整 `watch`。

### 5. 数值冲突的处理

对大小、范围、切除标本尺寸等数值冲突，采用以下规则：

- 优先保留没有占位符、没有明显截断、语义完整的版本
- 优先保留和 `watchResult/specimen` 更一致的版本
- 若某个数值字段在所有版本里都是残缺模板（例如 `标本大小约，`、`约数值10mL`），则：
  - 能从原文本直接恢复数字的，恢复为正常表述
  - 无法恢复真实数值的，不编造数值，删除无信息残句

## 典型处理方式

### 1. “正常” 与 “有病变” 冲突

保留有病变版本。例如：

- 一版写“未见肿块及息肉”
- 另一版写“升结肠见三枚息肉，直肠见一枚息肉”

则保留后者。

### 2. 一版漏掉超声或手术段

如果一版只有超声，另一版只有手术记录，则合并为：

- `内镜所见`
- `超声所见`
- `手术记录`

### 3. 一版只有半截模板

如果文本中只剩：

- `标本大小约，`
- `切除标本大小约*cm`
- `术中出血量约数值10mL`

则做两类处理：

- 能直接恢复数字的，改成正常文本
- 不能恢复的，删去这一小段残句

## 本次未做的事

- 未修改 `suggest` 列
- 未修改 `watchResult` 列
- 未修改任何训练代码、配置文件、脚本文件
- 未改动原始副本 `valid_dicts_report.csv`

## 实际改动文件

- 已修改：`/home/Lim/Project4/datasets/valid_dicts_report_for task2.csv`
- 已新增：`/home/Lim/Project4/src/TASK2_DATA.MD`
