# temp.py 详细说明

## 1. 脚本职责

`temp.py` 用于检查 EUS/GIST/Leiomyoma 数据集的目录结构，并生成两类输出：

1. 患者级有效性表格；
2. 清洗统计报告。

脚本会基于 `configs/path.yaml` 中维护的路径配置读取数据根目录与输出位置；若命令行显式传入路径参数，则以命令行参数为准。

## 2. 路径配置来源

项目中所有默认路径均统一维护在 `configs/path.yaml`：

- `paths.dataset_root`：原始数据集根目录；
- `paths.output_dir`：输出目录；
- `paths.patient_validity_table`：患者有效性表格输出文件；
- `paths.cleaning_report`：清洗统计报告输出文件。

因此，若后续调整目录结构，优先修改 `configs/path.yaml`，而不是直接修改脚本中的常量。

## 3. 统计逻辑

### 3.1 患者目录识别

- 数据根目录下的一级子目录会被视为患者目录；
- 患者目录名称默认期望以 `ZS` 开头；
- 若命名不符合约定，不会阻止统计，但会被记录为异常命名患者。

### 3.2 检查目录识别

- 每个患者目录下的直接子目录会被视为一次检查；
- 单次检查目录同时存在 `img/` 与 `pdf/` 时，判定为有效检查；
- 缺少任一子目录时，判定为结构异常检查，并在清洗后统计中剔除。

### 3.3 统计结果

脚本会输出两份统计结果：

- **原始统计结果**：基于患者目录下全部直接子目录计算；
- **img/pdf 清洗后的统计**：将缺少 `img` 或 `pdf` 的检查目录视为不存在后重新统计。

统计字段包括：

1. 患者总数；
2. 检查总次数；
3. 空患者目录数；
4. 命名异常患者目录数及样例；
5. 缺少 `img/pdf` 的检查目录数量及样例；
6. 患者检查次数分布。

## 4. 患者有效性表格

脚本会在输出目录中生成三列表格：

| patient_dir | is_valid | invalid_reason |
| --- | --- | --- |
| 患者目录名 | 1/0 | 无效原因说明 |

判定规则如下：

- 默认先将第二列 `is_valid` 视作有效；
- 仅当患者在 **img/pdf 清洗后的统计结果中检查次数为 0** 时，判定为无效，即 `is_valid = 0`；
- 若 `is_valid = 0`，则 `invalid_reason` 写为 `清洗后的患者分布为 0 次检查`；
- 其余患者的 `invalid_reason` 为空。

这意味着：患者命名是否符合 `ZS` 约定、以及是否存在被清洗掉的异常检查目录，都不会单独导致第二列被置为 `0`；只有清洗后该患者没有任何保留检查时，才会标记为无效。

## 5. 清洗统计报告

脚本会额外生成 `cleaning_report` 文件，当前包含以下统计内容：

- `原始数据：X名患者`
- `经过 img/pdf 清洗后的数据：Y名患者`

其中 `X` 与 `Y` 当前均统计患者总数，便于快速对照清洗前后的整体规模。

## 6. 使用方式

### 6.1 使用默认路径配置运行

```bash
python temp.py
```

### 6.2 覆盖 path.yaml 中的路径

可通过命令行参数覆盖默认配置，例如分别指定数据集根目录、患者有效性表格输出文件和清洗统计报告输出文件。

### 6.3 指定其他路径配置文件

如需切换到其他配置文件，可通过 `--config` 参数传入目标配置文件。

## 7. 输出文件说明

默认会生成以下两类结果文件：

- 患者有效性表格文件：保存患者级三列表格；
- 清洗统计报告文件：保存清洗前后患者数对比结果。

## 8. 维护建议

- 如需修改项目默认路径，请优先更新 `configs/path.yaml`；
- 如需新增更多输出文件，也建议继续在 `configs/path.yaml` 中补充路径参数；
- 若文档提到脚本细节，请统一引用本文件，避免在其他 `.md` 文件中重复展开 `temp.py` 的实现说明。
