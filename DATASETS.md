# 数据集结构说明

## 1. 数据集根目录

默认使用以下路径作为原始数据集根目录：

```text
/home/Lim/datasets/eus-gist-leiomyoma/
```

该路径应由多个患者目录组成。

## 2. 患者级目录结构

每个患者一个目录，目录名格式预期为：

```text
ZSxxxxxxxx
```

其中：

- `ZS` 为固定前缀；
- 后续字符通常为患者编号；
- 脚本会优先按该格式识别，但不会强制拒绝其他目录名，只会在统计中提示异常命名样例。

## 3. 检查级目录结构

每个患者目录下包含 `1~n` 个检查目录。每个检查目录表示该患者的一次检查，可能对应：

- 胃镜
- 无痛胃镜
- 超声胃镜
- 染色胃镜
- 肠镜
- 其他扩展检查

检查目录下应至少包含两个子目录：

```text
检查目录/
├── img/
└── pdf/
```

其中：

- `img/`：保存该次检查产生的图片；
- `pdf/`：保存该次检查对应的 PDF 报告。

## 4. 当前统计目标

项目当前主要统计以下内容：

1. 患者总数；
2. 总检查次数；
3. 做了 `n` 次检查的患者数量分布；
4. 缺少 `img` 或 `pdf` 的检查目录数量；
5. 基于原始目录统计的空患者目录数量；
6. 命名异常患者目录数量；
7. 将缺少 `img` 或 `pdf` 的检查目录剔除后的二次统计结果。

## 5. 统计口径

`temp.py` 采用如下口径：

- 患者数：数据根目录下的一级子目录数量；
- 原始统计中的检查数：每个患者目录下的直接子目录数量；
- 单次检查是否有效：若检查目录下同时存在 `img` 与 `pdf`，则视为有效检查；
- img/pdf清洗后的统计：把无效检查目录直接视为不存在，再重新统计检查总数、空患者数与检查次数分布；
- 分布统计：以“患者拥有的检查目录数量”为准进行聚合。

## 6. 使用建议

- 在正式清洗或建模前，先运行 `python temp.py` 获取当前数据结构概览；
- 若实际目录命名与 `ZSxxxxxxxx` 不完全一致，也可以先运行脚本查看异常目录样例，再决定是否统一重命名；
- 若后续还需要统计图片数、PDF 数、每种检查类型占比，可在当前脚本基础上继续扩展。
