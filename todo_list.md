# 医学内镜图像数据集清洗与标签噪声处理方案

## 问题分析

### 核心矛盾

数据标签是**患者级（patient-level）**的，由检查报告的 `reportTitle` 决定，但一个检查目录下的图像可能包含**多种模态/子类型**的图像。例如：

- "无痛胃镜(含色素内镜)" 目录下混有：标准白光胃镜图像 + 染色（色素）内镜图像
- "正常无痛胃镜" 目录下也可能混有染色内镜图像
- "超声胃镜" 目录下混有普通胃镜图像（用作对比参照）

**本质**：这是一个**标签粒度不匹配**问题——标签在患者/检查级别，但图像在实例级别存在异质性。

### 对你方案的评价

#### 方案一：用 check_similarity 做类型划分

- **优点**：已有现成工具，三种度量（Centroid Cosine / FID / MMD）可以从分布层面量化类间差异
- **局限**：check_similarity 是在 **reportTitle 类别层面** 做同质性分析，无法解决**类别内部的图像级异质性**。它能告诉你"无痛胃镜"和"染色胃镜"整体分布不同，但无法自动把混在"无痛胃镜"里的染色图像挑出来

#### 方案二：手动清洗 + 算法辅助剔除

- **优点**：最直接、最可控，质量上限最高
- **局限**：
  1. 剔除的图像去向不明——直接丢弃损失数据，重新归类缺乏判断标准
  2. 清洗后数据的下游使用方式不清晰——是否需要多分类器集成
  3. 工作量巨大且难以标准化复现

**总体评价**：两个方案都有价值但不完整。方案一适合做**预分析**，方案二适合做**小规模验证**，但都不是最终的系统化解决方案。

---

## 推荐方案（共6个）

---

### 方案一：多实例学习 MIL（Multi-Instance Learning）【业界成熟方案】

> **来源**：计算病理学（Computational Pathology）领域标准做法，如 CLAM (Lu et al., 2021)、ABMIL (Ilse et al., 2018)、TransMIL (Shao et al., 2021)

#### 核心思想

不清洗、不拆分。直接将每个检查目录视为一个 **bag（袋）**，目录下每张图像是一个 **instance（实例）**，标签只在 bag 级别。模型学习从一组异质图像中聚合信息做出 bag 级别的预测。

#### 具体步骤

1. **特征提取**：用预训练 ResNet/ViT（ImageNet 或医学预训练如 BiomedCLIP）对每张图像提取特征向量
2. **聚合模块**：用注意力机制（Attention-based MIL）对 bag 内所有 instance 加权聚合
3. **分类**：在聚合特征上做 bag 级别分类
4. **副产物**：注意力权重天然提供 instance 级别的重要性排序——高权重图像是对分类贡献最大的"典型"图像，低权重图像可能就是混入的异质图像

#### 优势

- **完全不需要手动清洗**，直接使用现有患者级标签
- 在计算病理学（WSI 分析）中已被广泛验证
- 注意力权重可解释，可反向用于数据质量分析
- 成熟框架多：CLAM、DSMIL、DTFD-MIL 等

#### 劣势

- 对"到底哪些图像属于哪个子类型"不做显式建模
- bag 内实例数差异大时需要采样策略

#### 适用场景

你的目标如果是**患者级分类**（如判断检查类型、诊断结果），这是最直接的方案。

---

### 方案二：噪声标签学习（Noisy Label Learning）【业界成熟方案】

> **来源**：DivideMix (Li et al., 2020, ICLR)、Co-teaching (Han et al., 2018, NeurIPS)、SOP (Liu et al., 2022)，已在 WebVision、Clothing1M 等真实噪声数据集上验证

#### 核心思想

将问题建模为**标签噪声（label noise）**问题：每张图像继承了患者级标签，但部分图像的真实类型与继承标签不符——这就是噪声标签。

#### 具体步骤

1. **初始训练**：用所有图像和继承标签正常训练图像分类器
2. **噪声识别**：通过以下方法识别噪声样本
   - **损失分布分析**：clean sample 的 loss 通常小于 noisy sample（小损失准则）
   - **GMM 拟合**：用 2-component GMM 拟合 per-sample loss 分布，分离 clean/noisy
   - **Co-teaching**：训练两个网络，互相选择小损失样本教对方
3. **噪声处理**：
   - **DivideMix**：clean 样本用标注标签，noisy 样本用 MixMatch 做半监督学习
   - **SOP**：为每个样本学习一个标签修正向量

#### 优势

- **直接在图像级别工作**，可以识别出哪些图像标签是"错误"的
- 理论成熟，方法众多
- 可以同时**训练分类器 + 发现噪声样本**

#### 劣势

- 需要定义图像级别的类别体系（不能只用 reportTitle）
- 如果噪声比例过高（如超过 40%），效果会显著下降

#### 适用场景

你的目标如果是训练**图像级分类器**（如区分白光胃镜图像 vs 染色内镜图像），且噪声比例可控。

---

### 方案三：自监督对比预训练 + 聚类伪标签迭代精炼【SOTA / 可发论文】

> **创新点**：结合自监督学习的无标签特征学习能力与聚类的自动子类型发现能力，设计一个**标签粒度自适应精炼框架**

#### 核心思想

不依赖现有的粗粒度标签来训练分类器，而是先用自监督学习获得高质量的图像表征，再通过聚类自动发现图像的细粒度子类型，最后将聚类结果与粗粒度标签对齐，生成精炼的图像级伪标签。

#### 具体步骤

**阶段一：自监督预训练**
1. 用 DINO v2 / MAE / MoCo v3 在**全部内镜图像**上做自监督预训练（不用任何标签）
2. 获得高质量的图像特征表征，自然地将视觉上相似的图像映射到特征空间的邻近位置

**阶段二：层次化聚类 + 子类型发现**
1. 对所有图像的特征做降维（UMAP）+ 聚类（HDBSCAN / Spectral Clustering）
2. 自动发现数据中的自然簇——这些簇对应"白光胃镜"、"染色内镜"、"超声图像"等视觉子类型
3. 用少量人工标注（如每簇标注 10-20 张）确定每个簇的语义含义

**阶段三：标签对齐与精炼**
1. 建立聚类簇与 reportTitle 类别之间的映射关系
2. 对每个检查目录：图像的聚类标签 + 检查级 reportTitle → 生成精炼的图像级标签
3. 例如："无痛胃镜(含色素内镜)" 目录下，聚类为"白光"的图像 → 标为"无痛胃镜-白光"；聚类为"染色"的图像 → 标为"无痛胃镜-染色"

**阶段四：迭代优化**
1. 用精炼标签训练有监督分类器
2. 分类器预测 → 与聚类标签投票 → 更新伪标签
3. 迭代 2-3 轮收敛

#### 创新点（论文价值）

- **无需大规模人工标注**，只需少量种子标注
- **标签粒度自动发现**：不预设子类型数量，由数据自然决定
- **从患者级标签到图像级标签的自动精炼**是一个novel的问题formulation
- 可以与 check_similarity 工具结合，用 FID/MMD 度量验证聚类质量

#### 论文标题建议

> "From Patient-Level to Instance-Level: Self-Supervised Label Granularity Refinement for Multi-Modal Medical Image Datasets"

---

### 方案四：层次化多标签学习 + 图像路由网络【SOTA / 可发论文】

> **创新点**：设计一个端到端的框架，显式建模"一个检查包含多种模态图像"的特性，同时解决图像分流（routing）和患者级分类

#### 核心思想

设计一个两阶段端到端网络：
- **第一阶段（Router）**：图像级模态路由器，自动判断每张图像属于什么模态/子类型
- **第二阶段（Aggregator）**：按模态分组后，每组用专家网络提取特征，最后融合做患者级预测

#### 网络架构

```
输入：一个检查目录的所有图像 {x_1, x_2, ..., x_N}
                    │
         ┌──────────▼──────────┐
         │   Image Router      │  ← 轻量分类器，判断每张图像的模态
         │   (模态分流网络)      │     如：白光/染色/超声/对比参照
         └──────────┬──────────┘
                    │
         ┌─────────┬┴────────┐
         ▼         ▼         ▼
    [白光图像]  [染色图像]  [超声图像]   ← 按模态分组
         │         │         │
    Expert_1   Expert_2   Expert_3      ← 模态专家网络
         │         │         │
         └─────────┼─────────┘
                   ▼
         ┌─────────────────┐
         │  Cross-Modal     │  ← 跨模态注意力融合
         │  Attention Fusion│
         └────────┬────────┘
                  ▼
         [患者级诊断预测]
```

#### 训练策略

**Router 的训练**（关键难点，因为没有图像级模态标签）：
1. **Bootstrap 初始化**：用 check_similarity 的 ResNet 特征 + K-means 生成初始伪模态标签
2. **Curriculum Learning**：先用高置信度样本训练 Router，逐步加入低置信度样本
3. **一致性正则化**：同一检查目录下，Router 分配的模态分布应与 reportTitle 的语义一致
   - 例如"超声胃镜检查报告"目录下，Router 应将大部分图像分配到"超声"模态

**Aggregator 的训练**：
- 用患者级标签（reportTitle / watchResult / hp 等）做监督
- 融合模块学习"哪个模态对当前诊断任务更重要"

#### 创新点（论文价值）

- **端到端联合学习**图像级模态识别 + 患者级诊断
- **Router + Expert 架构**类似 Mixture of Experts，但动机完全不同——是为了处理医学检查中的**多模态混合**问题
- **无需预先清洗**，Router 自动学习图像归属
- 天然支持**可解释性**：Router 的输出显示每张图像被归为哪种模态

#### 论文标题建议

> "Modality-Aware Routing Network for Patient-Level Diagnosis from Mixed-Modal Endoscopic Image Collections"

---

### 方案五：半监督图像级标注 + 主动学习（实用工程方案）

> **来源**：工业界数据标注最佳实践，结合 Active Learning (Settles, 2009) 与医学图像标注工具

#### 核心思想

不试图完全自动化，而是设计一个**高效的人机协同标注流程**，用最少的人工标注量获得高质量的图像级标签。

#### 具体步骤

**第一步：自动预标注**
1. 用你现有的 check_similarity 的 ResNet 特征
2. 对每个 reportTitle 类别内部做 K-means（K=2~5）
3. 将聚类结果作为"预标注"
4. 初步预估各簇的语义（如"白光"、"染色"、"超声"等）

**第二步：少量人工验证**
1. 每个簇随机抽样 20-30 张图像
2. 人工确认/修正簇的语义标签
3. 识别明显的错误聚类

**第三步：训练初始分类器**
1. 用已确认的样本训练一个图像级模态分类器
2. 对所有未标注图像做预测

**第四步：主动学习迭代**
1. 选择分类器**最不确定的样本**（如 entropy 最高的）
2. 人工标注这批样本（每轮 50-100 张）
3. 加入训练集，重新训练
4. 重复 3-5 轮，直到分类器在验证集上 accuracy > 95%

**第五步：全量标注**
1. 用训练好的分类器对全部图像做自动标注
2. 人工抽检 5%，确认质量

#### 优势

- **工作量可控**：总人工标注量约 500-1000 张（占总数据量 <1%）
- **质量可靠**：有人工把关，标签质量有保证
- **渐进式**：每一步都可以评估效果，决定是否继续
- **可复现**：标注标准明确，流程可文档化

#### 劣势

- 需要一定的人工参与
- 需要设计标注界面/工具

---

### 方案六：基于 Vision-Language Model 的零样本/少样本图像分类与清洗

> **来源**：借鉴遥感、零售商品识别等领域用 CLIP/BLIP 做数据清洗的做法

#### 核心思想

利用预训练的视觉-语言模型（如 BiomedCLIP、PubMedCLIP、或通用 CLIP），通过文本描述来对图像进行零样本分类，自动识别和分离不同模态的图像。

#### 具体步骤

1. **定义文本提示**：为每种图像模态设计文本描述
   ```
   "a white-light gastroscopy image"
   "a chromoendoscopy image with dye staining"
   "an endoscopic ultrasound image"
   "a narrow-band imaging (NBI) endoscopy image"
   ```

2. **零样本分类**：用 CLIP 计算每张图像与各文本提示的相似度

3. **置信度阈值过滤**：
   - 高置信度（>0.8）：自动归类
   - 中等置信度（0.5-0.8）：人工确认
   - 低置信度（<0.5）：标记为需复查

4. **Fine-tune 优化**（可选）：
   - 用高置信度样本微调 BiomedCLIP
   - 对中低置信度样本重新预测

#### 优势

- **几乎不需要标注数据**即可启动
- VLM 在医学图像上表现日趋成熟
- 可以设计非常细粒度的文本描述

#### 劣势

- 通用 CLIP 在专业医学图像上可能不够准确
- 需要精心设计 prompt
- BiomedCLIP 等模型需要 GPU 资源

---

## 推荐执行路径

### 短期（立即可做）

- [ ] **执行方案五的第一步**：利用现有 check_similarity 的 ResNet 特征，对每个 reportTitle 类别内部做 K-means 聚类，观察类别内部的子结构
- [ ] **小规模实验方案六**：用 BiomedCLIP 对 100 张样本做零样本分类，评估 VLM 在你的数据上的可用性
- [ ] **数据现状摸底**：抽样统计各 reportTitle 类别中"不匹配"图像的比例，量化噪声程度

### 中期（确定方向后）

- [ ] **如果目标是患者级分类** → 重点推进**方案一（MIL）**，这是最成熟且最匹配你当前数据结构的方案
- [ ] **如果目标是图像级分类** → 推进**方案五（主动学习）** 获取图像级标签，再结合**方案二（噪声标签学习）**
- [ ] **如果目标是发论文** → 推进**方案三**或**方案四**，它们有明确的创新点和故事线

### 长期（系统化建设）

- [ ] 建立完整的**图像级标注体系**（模态 + 解剖部位 + 病变类型）
- [ ] 构建**多模态患者级诊断模型**（融合不同模态的图像信息）
- [ ] 将数据清洗与模型训练形成**闭环**：模型预测 → 发现新的噪声 → 清洗 → 重新训练

---

## 各方案对比总结

| 方案 | 核心方法 | 需要额外标注 | 发论文潜力 | 工程复杂度 | 适用目标 |
|------|---------|-------------|-----------|-----------|---------|
| 一：MIL | 多实例学习 | 不需要 | 中 | 中 | 患者级分类 |
| 二：噪声标签 | DivideMix等 | 需要图像级类别定义 | 中 | 中 | 图像级分类 |
| 三：自监督+聚类 | DINO+聚类迭代 | 少量种子标注 | **高** | 高 | 标签精炼 |
| 四：路由网络 | Router+Expert | 不需要 | **高** | 高 | 端到端诊断 |
| 五：主动学习 | 人机协同标注 | 500-1000张 | 低 | 低 | 数据清洗 |
| 六：VLM零样本 | BiomedCLIP | 几乎不需要 | 中 | 低 | 快速预分类 |

---

## 关键参考文献

1. **MIL**: Lu et al., "Data-efficient and weakly supervised computational pathology on whole-slide images", Nature Biomedical Engineering, 2021 (CLAM)
2. **Noisy Label**: Li et al., "DivideMix: Learning with Noisy Labels as Semi-supervised Learning", ICLR 2020
3. **Self-supervised**: Oquab et al., "DINOv2: Learning Robust Visual Features without Supervision", TMLR 2024
4. **Active Learning**: Settles, "Active Learning Literature Survey", 2009; Smailagic et al., "MedAL: Accurate and Robust Deep Active Learning for Medical Image Analysis", IEEE ISBI 2018
5. **VLM Medical**: Zhang et al., "BiomedCLIP: A Multimodal Biomedical Foundation Model", arXiv 2023
6. **MoE**: Shazeer et al., "Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer", ICLR 2017
7. **TransMIL**: Shao et al., "TransMIL: Transformer based Correlated Multiple Instance Learning for Whole Slide Image Classification", NeurIPS 2021
